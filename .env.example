# 复制为 .env 并按需填写，真实凭据切勿提交到版本库。

# 开盘啦（Kaipanla）接口凭据，从 App 抓包获取。
# 后端通过 os.environ.get('KPL_*') 读取，缺失时抓取命令会因 Token 无效而失败。
KPL_USER_ID=
KPL_TOKEN=
KPL_DEVICE_ID=

# 前端开发时可选，指向本地 Django 后端。
# VITE_API_BASE=http://localhost:8000
