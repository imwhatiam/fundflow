# 复制为 .env 并按需填写，真实凭据切勿提交到版本库。

# 开盘啦（Kaipanla）可选 App 凭据；如需配置，应从本人获授权的 App 抓包获取。
# 后端仅从 os.environ.get('KPL_*') 读取。当前 RealRankingInfo 抓取会在变量非空时附带 UserID/Token，
# 但代码不强制它们存在；上游的匿名访问行为并非稳定契约。
KPL_USER_ID=
KPL_TOKEN=
KPL_DEVICE_ID=

# 前端开发时可选，指向本地 Django 后端。
# VITE_API_BASE=http://localhost:8000
